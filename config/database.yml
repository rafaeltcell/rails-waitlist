default: &default
  #username: waitlist
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: 5
  timeout: 5000

development:
  <<: *default
  host: localhost
  database: waitlist_dev
  password: <%= ENV['BD_DATABASE_PASSWORD'] || ""%>

test:
  <<: *default
  host: localhost
  database: waitlist_test
  password: <%= ENV['BD_DATABASE_PASSWORD'] || ""%>

#production:
  #<<: *default
  #database: waitlist
  #username: laksdjf
  #password: <%= ENV['RUBY-GETTING-STARTED_DATABASE_PASSWORD'] %>
